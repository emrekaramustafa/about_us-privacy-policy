import 'package:flutter_test/flutter_test.dart';
import 'package:goz_testi/main.dart';

void main() {
  testWidgets('App should start', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    // await tester.pumpWidget(const GozTestiApp());

    // Verify app loads
    // expect(find.text('Göz Testi'), findsOneWidget);
  });
}

